apiVersion: v1
kind: Service
metadata:
  name: {{ include "dkonsole.fullname" . }}
  labels:
    {{- include "dkonsole.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dkonsole.selectorLabels" . | nindent 4 }}
